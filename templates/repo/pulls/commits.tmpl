{{template "base/head" .}}
<link rel="stylesheet" href="{{AssetUrlPrefix}}/css/pull-commits.css">
<div role="main" aria-label="{{.Title}}" class="page-content repository view issue pull commits github-pull-review">
	<div class="ui container">
		{{template "repo/issue/view_title" .}}
		{{template "repo/pulls/tab_menu" .}}
		{{template "repo/commits_table" .}}
	</div>
</div>
{{template "base/footer" .}}
